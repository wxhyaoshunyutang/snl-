program c
type
     w2=record
          integer  x;
          integer  y;
        end;
var 
    integer x,y,z;
	array[1..20] of integer a;
    char m;
    w2 l;
procedure f(integer x,y;var integer z);
    begin
	z:=x+y+z;
	write(x);
	write(y);
	write(z)
    end
begin
x:=y/x;
if a < 1
then t:=a[k];
a[k]:=a[k+1];
a[k+1]:=t
else t:=0
fi;
return
end.